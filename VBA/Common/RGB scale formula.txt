Formula to get RGB color code on scale of 0 to 100. 0 = red, 100 = green

xvar = variable integer between 0 and 100 used to designate percentage


RGB(255 - ((255 / 100) * xvar), (255 / 100) * xvar, 0)